import { PlaceholderPage } from "../components/ui/PlaceholderPage";

export function TablesPage() {
  return (
    <PlaceholderPage
      title="Tables"
      description="Restaurant floor view with per-table status and order routing."
      phase="Phase 6"
    />
  );
}
